import { prisma } from './src/lib/prisma'; async function main() { const unassigned = await prisma.profile.findMany({ where: { cohorts: { none: {} } } }); console.log('Found ' + unassigned.length + ' unassigned students.'); if (unassigned.length > 0) { const firstCohort = await prisma.cohort.findFirst(); if (firstCohort) { for (const student of unassigned) { await prisma.profile.update({ where: { id: student.id }, data: { cohorts: { connect: [{ id: firstCohort.id }] } } }); console.log('Assigned ' + student.studentId + ' to ' + firstCohort.name); } } } } main().catch(console.error).finally(() => prisma.$disconnect());
